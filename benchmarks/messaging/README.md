# Messaging domain (stub)

Planned corpus case for chat / email / queue MCP servers.

Fill with `baseline.json`, `safe-change.json`, `breaking-change.json`,
`probes.json`, and `expected-results.json` — see [../README.md](../README.md).
